# BNB Greenfield E2E 测试配置
#
# 本文件包含运行完整端到端 Greenfield 存储测试所需的环境变量。
#
# 将此文件复制到 .env 并修改为你的实际值。
#
# 有关详细设置说明，请参阅：docs/GREENFIELD_E2E_使用指南.md

# ===== Greenfield 网络配置 =====
#
# 必需：测试网 RPC 端点，用于 CreateObject 交易
GREENFIELD_RPC_URL=https://gnfd-testnet-fullnode-tendermint-us.bnbchain.org

# 必需：存储提供者端点，用于 PutObject/GetObject 操作
GREENFIELD_SP_HOST=gnfd-testnet-sp3.bnbchain.org

# 必需：链 ID（测试网为 5600，主网为 1017）
GREENFIELD_CHAIN_ID=5600
GREENFIELD_CLI_CHAIN_ID=greenfield_5600-1

# 必需：你的存储桶名称（首先在 DCellar 中创建）
GREENFIELD_BUCKET=your-test-bucket

# 必需：用于签名交易的私钥（不含 0x 前缀）
# 从 MetaMask 导出：设置 → 安全与隐私 → 显示私钥
# 警告：仅使用测试网密钥！切勿在测试中使用主网密钥。
GREENFIELD_PRIVATE_KEY=your_private_key_without_0x_prefix

# ===== 测试配置 =====
#
# 可选：请求超时时间（秒，默认值：60）
GREENFIELD_TIMEOUT=60

# 可选：故障切换测试的备用 SP
GREENFIELD_SP_HOST_ALT=gnfd-testnet-sp1.bnbchain.org

# 可选：使用 gnfd-cmd 自动 CreateObject
GREENFIELD_CLI_BYPASS_SEAL=1
GREENFIELD_CREATE_OBJECT_CMD_TEMPLATE="bin/gnfd-cmd_mac --chainId {chain_id} --rpcAddr {rpc_url} --host {sp_host} --passwordfile {passwordfile} object put --bypassSeal --contentType {content_type} {file} gnfd://{bucket}/{object}"

# ===== 必需设置步骤 =====
#
# 1. 获取测试网 BNB：
#    访问：https://gnfd-bsc-testnet-faucet.bnbchain.org/
#    连接你的钱包并请求测试网 BNB
#
# 2. 创建测试存储桶：
#    访问：https://dcellar.io/（选择测试网网络）
#    点击"创建存储桶"
#    - 名称：设置为上面的 GREENFIELD_BUCKET
#    - 主 SP：选择 SP1（或使用 GREENFIELD_SP_HOST）
#    - 可见性："公开读取"（用于测试公开访问）
#    - 付款账户：你连接的钱包
#
# 3. 配置私钥：
#    在 MetaMask 中：设置 → 安全与隐私 → 显示私钥
#    复制密钥（如果存在则移除 0x 前缀）
#    设置为上面的 GREENFIELD_PRIVATE_KEY
#
# 4. 验证配置：
#    检查：
#    - GREENFIELD_BUCKET 与你创建的存储桶名称匹配
#    - GREENFIELD_PRIVATE_KEY 是正确的 64 字符十六进制字符串
#    - 你的钱包拥有用于燃气费的测试网 BNB
#
# ===== 运行 E2E 测试 =====
#
# 完成上述设置后：
#
# 1. 将此文件复制到 .env：
#    cp .env.e2e.示例 .env
#
# 2. 运行所有 E2E 测试：
#    pytest tests/test_greenfield_e2e.py -v -m integration -s
#
# 3. 运行特定测试：
#    pytest tests/test_greenfield_e2e.py::TestGreenfieldE2E::test_create_and_upload_small_text -v -s -m integration
#
# 4. 使用调试输出运行：
#    pytest tests/test_greenfield_e2e.py -v -s --log-cli-level=DEBUG -m integration
#
# ===== 预期测试结果 =====
#
# 所有 8 个 E2E 测试应该通过：
# - 测试 1：小文本上传和下载 ✅
# - 测试 2：JSON 声誉数据 ✅
# - 测试 3：二进制数据完整性 ✅
# - 测试 4：大数据性能 ✅
# - 测试 5：多对象工作流 ✅
# - 测试 6：事务哈希唯一性 ✅
# - 测试 7：错误处理 ✅
# - 测试 8：并发操作 ✅
#
# ===== 性能预期 =====
#
# 对于 1MB 测试数据：
# - 上传时间：10-60 秒（根据网络拥堵而变化）
# - 下载时间：1-10 秒
# - 上传速度：5-100 KB/s
# - 下载速度：50-500 KB/s
#
# ===== 故障排除 =====
#
# 如果测试失败：
# 1. 检查测试网 BNB 余额：https://greenfieldscan.com/address/YOUR_WALLET
# 2. 验证存储桶在 DCellar 中存在
# 3. 检查 SP 状态：https://greenfieldscan.com/
# 4. 如需要则等待网络传播
# 5. 查阅 GREENFIELD_E2E_使用指南.md 获取详细故障排除
#
# ===== 参考资料 =====
#
# - DCellar：https://dcellar.io/
# - 测试网水龙头：https://gnfd-bsc-testnet-faucet.bnbchain.org/
# - Greenfield 浏览器：https://greenfieldscan.com/
# - E2E 测试指南：docs/GREENFIELD_E2E_使用指南.md
# - Greenfield 文档：https://docs.bnbchain.org/bnb-greenfield/
